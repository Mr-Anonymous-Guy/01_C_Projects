#include <signal.h>
#include <stdio.h>
#include <stdlib.h>

void setup_signal_handlers(void) {
    /* Setup standard non-interactive handlers here, 
     * interactive ones are handled in init_shell_terminal */
}
